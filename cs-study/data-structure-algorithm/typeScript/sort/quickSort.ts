import { testArr1, testArr2 } from "../data";

function quickSort(numArr: number[]): number[] {
  let arr = [...numArr];

  return arr;
}

console.log(quickSort(testArr1));
console.log(quickSort(testArr2));
